{
  "name": "aws-image-optimization",
  "version": "1.0.0",
  "description": "Image optimization Lambda function",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "npm install --os=linux --cpu=x64 sharp && zip -r function.zip index.js node_modules package.json"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.0.0",
    "sharp": "^0.33.5"
  }
}
